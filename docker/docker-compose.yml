version: '3.8'

services:
  video-renderer:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ../output:/app/output
    env_file:
      - ../.env.local
    restart: unless-stopped
